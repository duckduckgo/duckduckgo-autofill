{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "StartCredentialsImportFlowResponse",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "type": {
        "description": "Required on Windows and MacOS",
        "type": "string",
        "const": "startCredentialsImportFlowResponse"
      },
      "success": {
        "description": "New `availableInputTypes` and `credentials` if import had credentials during import",
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "credentials": {
            "type": "array",
            "items": {
              "$ref": "./credentials.json"
            }
          },
          "availableInputTypes": { "$ref": "./availableInputTypes.json" }
        },
        "required": ["credentials", "availableInputTypes"]
      },
      "error": {
        "$ref": "./error.json"
      }
    }
  }
  